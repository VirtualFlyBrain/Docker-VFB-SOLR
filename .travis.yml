services:
  - docker
  
script:
  - docker build -t test-image:test .
  - docker run -d --name test -p 8983:8983 test-image:test
  - sleep 2m
  - docker logs test
  - curl -sSf http://localhost:8983/solr/ontology/select
